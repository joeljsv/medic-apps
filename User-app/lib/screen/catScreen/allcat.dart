import 'package:flutter/material.dart';

class AllCateGoriesSymptom extends StatelessWidget {
  const AllCateGoriesSymptom({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("All Categories"),
      ),
    );
  }
}